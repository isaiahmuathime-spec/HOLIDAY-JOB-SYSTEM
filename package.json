{
  "name": "holiday-job-system",
  "version": "1.0.0",
  "private": true,
  "description": "Holiday Job System backend server and frontend integration.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "frontend": "echo 'Frontend is served by the backend. Run npm start or npm dev to serve both.'",
    "install-all": "echo 'Backend dependencies installed. Frontend uses CDN links, no build step required.'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.1.4",
    "express": "^4.18.4",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^9.8.1",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
